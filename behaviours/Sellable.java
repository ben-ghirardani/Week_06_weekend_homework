package behaviours;

public interface Sellable {

  int calculateMarkup();

  // Need to include a calculateMarkup() method to every item that will be sellable. 
  // Need to include a buying and selling price to them. 
  // Need to work out how to implement two interfaces.

}